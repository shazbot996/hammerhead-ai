# Python artifacts
__pycache__/
*.py[cod]
*.so

# Environment folders
.venv
venv/
env/

# IDE / Editor specific
# This is important because your sftp.json contains a private key path.
.vscode/

# Project-specific ignores
# ========================

# Ignore the entire local data cache directory.
# This is CRITICAL as it contains the config.json with secrets,
# the GCP service account key, and all cached data (images, encodings).
local_data_cache/
# ignore my source example code
pycoral/

# Ignore any temporary audio files generated by the application
temp_response-*.wav
temp_response-*.mp3

# A general rule to catch the service account key if it's ever
# moved outside the cache directory.
gcp-service-account.json