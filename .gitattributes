# This file ensures consistent line endings across different operating systems.

# Set the default behavior for all text files to be normalized by Git.
* text=auto

# Enforce LF line endings for file types that will be run on Linux.
*.py text eol=lf
*.sh text eol=lf
